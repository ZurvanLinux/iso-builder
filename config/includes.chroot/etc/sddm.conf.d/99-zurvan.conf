# Zurvan Linux SDDM defaults.
# Wayland session by default (technical-spec-architecture.md §3.1); X11 remains
# available as a fallback from the SDDM session picker.
# Auto-login the live user straight to the Plasma Wayland session.
[Autologin]
User=user
Session=plasma

[Theme]
Current=breeze
