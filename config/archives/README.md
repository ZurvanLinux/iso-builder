# Zurvan custom repo GPG keyring placeholder.
# Drop the Zurvan archive public key here as zurvan-archive-keyring.gpg
# (binary GPG format: gpg --dearmor < key.asc > zurvan-archive-keyring.gpg)
# when repo.zurvanlinux.org goes live (Phase 2).
