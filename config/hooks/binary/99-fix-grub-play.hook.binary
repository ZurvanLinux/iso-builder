#!/bin/sh
set -e

if [ -d binary ]; then
    find binary -name "grub.cfg" -type f | while read -r cfg; do
        grep -q 'function play' "$cfg" || sed -i '1i function play { true; }' "$cfg"
    done
fi